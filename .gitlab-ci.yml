stages:
  - build

variables:
  PACKAGE_NAME: "ai-guidelines-repo"

build-archive:
  stage: build
  image: alpine:latest
  script:
    - touch "app-version_${CI_COMMIT_SHORT_SHA}"
  artifacts:
    name: "${PACKAGE_NAME}"
    paths:
      - web/
      - "app-version_${CI_COMMIT_SHORT_SHA}"
